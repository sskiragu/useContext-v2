import { Typography } from "@mui/material";

function Footer(){
    return(
        <div>
            <footer>
                <Typography variant="h1" color="primary">All rights reserved.</Typography>
            </footer>
        </div>
    )
}

export default Footer;